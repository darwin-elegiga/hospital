import { FilterSexoPipe } from './filter-sexo.pipe';

describe('FilterSexoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSexoPipe();
    expect(pipe).toBeTruthy();
  });
});
