import { FilterSalaPipe } from './filter-sala.pipe';

describe('FilterSalaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSalaPipe();
    expect(pipe).toBeTruthy();
  });
});
